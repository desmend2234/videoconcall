// import { create } from 'zustand';

// interface IUser {
//   fullName: string;
//   setFullName: (name: string) => void;
// }

// export const useUser = create<IUser>((set) => ({
//   fullName: '',
//   setFullName: (name: string) => set({ fullName: name }),
// }));
// // This hook is used to store the user's name. It uses Zustand to create a store with the user's name.
// export default useUser;

//用clerk useUser的資料取代zustand的資料
